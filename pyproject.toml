[project]
name = "plex-library-watch-history"
version = "0.1.0"
description = "Show stale and unwatched items in plex from tautulli and seerr"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pydantic-settings>=2.14.2",
    "requests>=2.34.2",
    "rich>=15.0.0",
]
